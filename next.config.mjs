/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    reactStrictMode: true,
    // basePath: '',
    // assetPrefix: "",
    experimental: {
        reactCompiler: true,
    },
};

export default nextConfig;
